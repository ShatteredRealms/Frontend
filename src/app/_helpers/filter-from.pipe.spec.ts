import { FilterFromPipe } from './filter-from.pipe';

describe('FilterFromPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFromPipe();
    expect(pipe).toBeTruthy();
  });
});
